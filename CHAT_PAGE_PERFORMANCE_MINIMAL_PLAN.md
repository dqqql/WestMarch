# 逸闻趣事（Chat）线上慢加载最小化修复方案

## 1. 现状与已确认结论

- 线上体感：`messages` 约 `17s`，右侧卡片跟着慢。
- 服务器本机直连实测（`127.0.0.1:3000`）：
  - `messages`：`0.17~0.21s`，但响应体 `8573138 bytes`（约 `8.6MB`）
  - `items`：`~0.005s`，响应体 `2 bytes`
  - `parties`：`~0.005s`，响应体 `3109 bytes`
- 数据库排查：该据点消息只有 5 条，正文总长度很小；`Character.img` 中存在大量 `data:image/...;base64,...`（2MB~4MB+ 单条）。

## 2. 根因（按优先级）

1. `messages` 接口返回了超大 base64 图片字段（`character.img`），导致公网传输慢。  
2. 前端当前请求链路是“先等 `messages`，再请求 `items/parties`”，放大了体感等待。

## 3. 最小化改动原则

- 不改数据库结构，不做数据迁移，不改部署架构。
- 只改 2 处代码，优先解决 90% 体感问题。
- 保持功能可用：不影响发消息、发物品、发组队、加入组队。

## 4. 建议改动（最小集）

## 改动 A（后端，最高优先）
- 文件：`src/repositories/ChatRepository.ts`
- 方法：`findMessagesByNodeAndChannel`
- 动作：消息查询里不返回 `character.img`（以及其他可能的大字段）。
- 说明：当前消息列表仅需角色标识信息（如 `id/name/race/class`），头像可用默认占位或后续单独加载。

## 改动 B（前端，次优先）
- 文件：`src/app/chat/page.tsx`
- 方法：`loadChannelData`
- 动作：把 `messages/items/parties` 三个请求改为同一轮并行 `Promise.all`，不要等待 `messages` 返回后再发右侧请求。
- 说明：即使 `messages` 偶尔慢，右侧卡片也可先显示，明显改善“页面卡住”的主观体验。

## 5. 预估收益（基于当前实测）

## 已知基线
- 当前 `messages`：`8.6MB`，公网体感约 `17s`。
- 推算链路吞吐约 `0.5MB/s`（`8.6MB / 17s`）。

## 改动 A 后（仅去掉 base64 图片字段）
- 预估 `messages` 响应体可降到 `20KB~120KB`（与消息条数、字段数量相关）。
- 在同等链路下，传输耗时可从 `~17s` 降到 `~0.05s~0.25s`（仅网络传输部分）。
- 加上服务端处理与前端解析，总体预估：
  - `messages` 端到端：`17s -> 0.4s~2s`
  - 提升幅度：约 `8x~40x`

## 改动 B 后（并行请求）
- 右侧卡片不再被 `messages` 阻塞，通常可在 `<1s` 内展示（当前服务端实测 `~0.005s`）。
- 主观体感：页面“先出内容、后补消息”，首屏等待显著降低。

## 6. 验证口径（改后必须复测）

1. `curl` 本机直连：
   - 记录 `messages/items/parties` 的 `ttfb/total/size_download`
2. 浏览器 Network（公网）：
   - 对比改前改后的 `messages` 体积与总耗时
3. 页面体验：
   - 进入“逸闻趣事”后，右侧卡片是否能快速出现
4. 回归功能：
   - 发消息、发物品、发组队、加入组队、关闭组队

## 7. 风险与兼容性

- 若前端消息列表依赖 `message.character.img` 渲染头像，去掉后会显示默认头像（功能不受影响，属于视觉退化）。
- 为避免影响其他页面，可加“紧凑模式”参数（如 `compact=1`）仅供 chat 页使用；这会多一点代码，但兼容性更稳。

## 8. 后续（非本次最小集）

- 把 base64 图片逐步迁移为文件 URL（对象存储/静态资源），数据库只存 URL。
- 给消息接口加分页（如默认 30 条）与“加载更多”。
- 开启/确认反向代理 gzip 或 brotli（对 JSON 有额外收益）。

